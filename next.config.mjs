/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "startup-template-sage.vercel.app",
      "avatar.vercel.sh",
      "tailwindui.com",
    ],
  },
};

export default nextConfig;
